const AppStyles = {
  statusBarColor: '#10346a',
  lightGradientColor: '#154285',
  themeColor: 'red',
  themeComp: '#009D80',
  textColor: 'white',
};

const AppTheme = () => {
  return AppStyles;
};

export default AppTheme;
